<% layout('../layout/index') -%>
<% const {MaBN, HoTen, NamSinh, GioiTinh, DiaChi} = BenhNhan[0] %>
<title>Sửa bệnh nhân <%= HoTen%> - Quản lý phòng mạch tư</title>

<% stylesheet('assets/plugins/switchery/css/switchery.min.css') -%>
<% stylesheet('assets/plugins/select2/css/select2.min.css') -%>
<% stylesheet('assets/plugins/bootstrap-select/css/bootstrap-select.min.css') -%>

<div class="row">
    <div class="col-sm-12">
        <ol class="breadcrumb">
            <li>
                <a href=""><i class="ti-home"></i></a>
            </li>
            <li>
                <a href="/benhnhan">Danh sách bệnh nhân</a>
            </li>
            <li class="active">
                Sửa bệnh nhân <%= HoTen%>
            </li>
        </ol>
    </div>
</div>

<!-- @if (count($errors) > 0 || session('error'))
        <div class="alert alert-danger" role="alert">
            <strong>Cảnh báo!</strong><br>
            @foreach($errors->all() as $err)
                {{$err}}<br/>
            @endforeach
            {{session('error')}}
        </div>
    @endif -->
<!-- @if (session('success'))
        <div class="alert alert-success">
            <strong>Thành công!</strong>
            <button type="button" class="close" data-dismiss="alert">×</button>
            <br/>
            {{session('success')}}
        </div>
    @endif -->

<!--end duong dan nho-->

<div class="row">
    <div class="col-sm-12">
        <div class="card-box">
            <h4 class="m-t-0 header-title"><b>Sửa bệnh nhân <%= HoTen%></b></h4>
            <p class="text-muted m-b-10 font-13">
                <b>Bắt buộc</b> <code>Họ & tên</code> <code>Giới tính</code> <code>Năm sinh</code> <code>Địa chỉ</code>
            </p>
            <div class="row">
                <div class="col-md-6">
                    <div class="p-l-r-10">
                        <form class="form-horizontal" role="form" action="" method="post">

                            <div class="form-group">
                                <label class="control-label">Họ & tên</label>
                                <input name="hoten" type="text" class="form-control" value="<%= HoTen%>"
                                    placeholder="Nhập họ tên..." required>
                            </div>

                            <div class="form-group">
                                <label class="control-label">Giới tính</label>
                                <select class="selectpicker" data-style="btn-default btn-custom" id="gioitinh"
                                    name="gioitinh">
                                    <option value="">--- Chọn giới tính ---</option>
                                    <option value="1" <% if (GioiTinh == 1) { %> selected <% } %>>Nữ</option>
                                    <option value="2" <% if (GioiTinh == 2) { %> selected <% } %>>Nam</option>
                                    <option value="3" <% if (GioiTinh == 3) { %> selected <% } %>>Khác</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label class="control-label">Năm sinh</label>
                                <select class="selectpicker" data-style="btn-default btn-custom" id="namsinh"
                                    name="namsinh">
                                    <option value="">--- Chọn năm sinh ---</option>
                                    <% for (var i = 2019; i >= 1920; i--) { %>
                                    <option value="<%= i%>" <% if (NamSinh == i) { %> selected <% } %>> <%= i%>
                                    </option>
                                    <% } %>
                                </select>
                            </div>

                            <div class="form-group">
                                <label class="control-label">Địa chỉ</label>
                                <input name="diachi" type="text" class="form-control" value="<%= DiaChi%>"
                                    placeholder="Nhập địa chỉ..." required>
                            </div>

                            <div class="form-group">
                                <button class="ladda-button btn btn-default" data-style="expand-right">Lưu lại
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<% script('assets/plugins/switchery/js/switchery.min.js') -%>